import { configureStore } from '@reduxjs/toolkit'

// Reducers added here as modules are built
// import authReducer from '../modules/auth/store/auth.slice'

export const store = configureStore({
  reducer: {},
})

export default store
